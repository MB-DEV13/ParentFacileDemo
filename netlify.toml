# netlify.toml — Build du frontend Vite
[build]
  command = "npm run build"
  publish = "dist"

[dev]
  command = "npm run dev"
  port = 5174
  targetPort = 5174

[template.environment]
  # ⚠️ Ajuste cette URL après déploiement du backend Render
  VITE_API_BASE = "https://parentfacile-backend.onrender.com"
